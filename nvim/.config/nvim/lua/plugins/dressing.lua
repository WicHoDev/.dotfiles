return{
    "stevearc/dressing.nvim",
    event = "VeryLazy",
    opts = {
        input = {
            enabled = false, -- 👈 disable to avoid 'text' popups
        },
        select = {
            enabled = false,
        },
    }
}
